---
phase: 02-core-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/App.tsx, src/components/Dashboard.tsx, src/components/Dashboard.css, src/components/TransferModal.tsx, src/components/TransferModal.css]
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03]
must_haves:
  truths:
    - "User can select a giver and receiver by clicking player cards"
    - "Points are transferred correctly and sum remains 0"
    - "Selected players are visually highlighted (Red for Giver, Green for Receiver)"
  artifacts:
    - path: "src/components/TransferModal.tsx"
      provides: "UI for entering custom transfer amounts"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/TransferModal.tsx"
      via: "State-driven rendering"
---

<objective>
Implement the core zero-sum scoring logic, the two-click player selection flow, and the custom amount transfer modal.

Purpose: To provide the essential point transfer mechanic for the application.
Output: Working score transfer system with visual feedback and strict validation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-scoring/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Selection State & Highlighting</name>
  <files>src/App.tsx, src/components/Dashboard.tsx, src/components/Dashboard.css</files>
  <action>
    Add `giverIndex` and `receiverIndex` state to `App.tsx`.
    Implement `handlePlayerClick` logic in `App.tsx`:
    - First click sets `giverIndex`.
    - Second click (on different player) sets `receiverIndex` and opens TransferModal.
    - Click on already selected player cancels selection.
    Pass selection status to `Dashboard.tsx` and apply `giver-selected` and `receiver-selected` CSS classes.
    Update `Dashboard.css` with red/green tints and borders for these states (D-2-04).
  </action>
  <verify>
    <automated>grep -q "giverIndex" src/App.tsx</automated>
  </verify>
  <done>Selection flow is functional with visual feedback on player cards.</done>
</task>

<task type="auto">
  <name>Task 2: Create TransferModal Component</name>
  <files>src/components/TransferModal.tsx, src/components/TransferModal.css</files>
  <action>
    Create `TransferModal.tsx` that receives `giverName`, `receiverName`, and an `onTransfer` callback.
    Implement a simple overlay modal with a numeric input for the transfer amount (D-2-02).
    Add "Transfer" and "Cancel" buttons.
    Ensure "Transfer" is disabled if amount is 0 or negative.
  </action>
  <verify>
    <automated>grep -q "TransferModal" src/App.tsx</automated>
  </verify>
  <done>Transfer modal allows users to specify point amounts for the transfer.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Zero-Sum Scoring Logic</name>
  <files>src/App.tsx</files>
  <action>
    Implement `performTransfer` function in `App.tsx`:
    - Takes `giverIndex`, `receiverIndex`, and `amount`.
    - Updates scores: `giverScore -= amount`, `receiverScore += amount`.
    - Resets selection states and closes modal.
    - Ensures Σ scores = 0 is maintained (D-2-03).
  </action>
  <verify>
    <automated>grep -q "\-=" src/App.tsx</automated>
  </verify>
  <done>Zero-sum point transfers work correctly and update the dashboard instantly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Modal Input | Untrusted numeric input for transfer amount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | TransferModal.tsx | mitigate | Sanitize and convert input to number; ensure it's positive before passing to transfer logic |
| T-02-02 | Denial of Service | App.tsx | mitigate | Ensure large number inputs don't crash the calculation (standard JS number handling) |
</threat_model>

<verification>
- Start the game and click a player; verify they are highlighted.
- Click a second player; verify the modal opens with both names.
- Enter "5" and transfer; verify giver score is -5 and receiver is +5.
- Total sum of all players must equal 0.
</verification>

<success_criteria>
- Transfer flow (select giver -> select receiver -> modal) is seamless.
- Scores reflect transfers accurately.
- UI highlights selected players clearly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-scoring/02-01-SUMMARY.md`
</output>
