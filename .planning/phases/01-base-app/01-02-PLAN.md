---
phase: 01-base-app
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/components/Dashboard.tsx, src/components/Dashboard.css, src/App.tsx]
autonomous: true
requirements: [SET-02, SET-03]
must_haves:
  truths:
    - "Dashboard shows 4 players in a 2x2 grid"
    - "All players start with 0 points"
    - "UI is responsive on mobile"
  artifacts:
    - path: "src/components/Dashboard.tsx"
      provides: "Score display grid"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/Dashboard.tsx"
      via: "Props passing"
---

<objective>
Implement the core game dashboard with 2x2 grid layout and player initialization.

Purpose: To provide the primary scoring interface for the game.
Output: Responsive 2x2 grid showing the 4 players and their starting scores (0).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-base-app/01-CONTEXT.md
@.planning/phases/01-base-app/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 2x2 Dashboard Grid</name>
  <files>src/components/Dashboard.tsx, src/components/Dashboard.css</files>
  <action>
    Create `Dashboard.tsx` that receives `players` (array of objects with name and score).
    Implement a 2x2 grid layout using CSS Grid (D-03, SET-03).
    Style each player card with Horse/Earthy colors (D-04).
    Display the player name and their current score (initialized to 0, SET-02).
  </action>
  <verify>
    <automated>grep -q "display: grid" src/components/Dashboard.css</automated>
  </verify>
  <done>Dashboard renders 4 players in a responsive 2x2 grid.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive Polish & Mobile Testing</name>
  <files>src/components/Dashboard.css, src/index.css</files>
  <action>
    Ensure the 2x2 grid adapts well to portrait and landscape (SET-03).
    Use media queries to adjust font sizes or spacing if necessary for small screens.
    Ensure "Reset/Back to Setup" functionality exists in App.tsx to return to the setup screen if needed (internal logic).
  </action>
  <verify>
    <automated>grep -q "@media" src/components/Dashboard.css</automated>
  </verify>
  <done>UI is confirmed to be responsive and scores start at 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App State -> Dashboard | Trusted internal state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Dashboard.tsx | mitigate | Ensure score display is read-only from the dashboard perspective; all changes go through central state |
</threat_model>

<verification>
- Start the game with 4 names and verify the dashboard appears.
- Confirm all 4 players show "0" points.
- Check layout in a mobile emulator or resize browser.
</verification>

<success_criteria>
- Dashboard correctly displays the 4 player names.
- All scores are initialized to 0.
- 2x2 grid layout is maintained on mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-app/01-02-SUMMARY.md`
</output>
