---
phase: 01-base-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, vite.config.ts, src/main.tsx, src/App.tsx, src/App.css, src/index.css]
autonomous: true
requirements: [SET-01]
must_haves:
  truths:
    - "Application boots and shows the player setup screen"
    - "User can enter 4 names and start the game"
  artifacts:
    - path: "src/App.tsx"
      provides: "Main application entry and state management"
    - path: "src/App.css"
      provides: "Horse/Earthy theme variables and styles"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/SetupScreen.tsx"
      via: "Component rendering"
---

<objective>
Scaffold the React TypeScript project and implement the initial player setup screen with strict validation.

Purpose: To establish the application foundation and allow users to name the 4 players.
Output: Working setup screen with validation and the core Horse/Earthy theme.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-base-app/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold React Project</name>
  <files>package.json, tsconfig.json, vite.config.ts, src/main.tsx, src/index.css</files>
  <action>
    Use `npm create vite@latest . -- --template react-ts` to initialize the project in the current directory.
    Install dependencies: `npm install`.
    Set up `src/index.css` with the "Horse/Earthy" theme variables (D-04):
    - `--color-earth-brown: #4d3a29;`
    - `--color-stable-green: #2c5e1a;`
    - `--color-sand: #f4ece2;`
    - `--color-text: #2b2b2b;`
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Vite project initialized and buildable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Setup Screen with Validation</name>
  <files>src/components/SetupScreen.tsx, src/components/SetupScreen.css, src/App.tsx</files>
  <action>
    Create `SetupScreen.tsx` with 4 independent text inputs (D-01).
    Implement strict validation (D-02, SET-01):
    - "Start Game" button is disabled if any name is empty.
    - "Start Game" button is disabled if names are not unique.
    - Show error message for duplicates.
    Update `App.tsx` to manage the `players` state and show `SetupScreen` when `gameStarted` is false.
  </action>
  <verify>
    <automated>grep -q "SetupScreen" src/App.tsx</automated>
  </verify>
  <done>Setup screen allows entering 4 unique names and transitioning to the game.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Input Fields | Untrusted string input for player names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | SetupScreen.tsx | mitigate | Trim names and prevent duplicate/empty values before starting game state |
| T-01-02 | Info Disclosure | LocalStorage | accept | Score tracking data is low-value and local to the device |
</threat_model>

<verification>
- Verify that 4 unique names must be entered to enable the Start button.
- Verify the Horse/Earthy colors are applied to the setup screen.
</verification>

<success_criteria>
- Project builds without errors.
- 4 player names can be entered and validated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-app/01-01-SUMMARY.md`
</output>
