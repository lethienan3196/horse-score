---
phase: 04-persistence-history
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/App.tsx, src/components/HistoryScreen.tsx, src/components/HistoryScreen.css, src/components/Dashboard.tsx, src/components/SetupScreen.tsx]
autonomous: true
requirements: [SAVE-02, SAVE-03]
must_haves:
  truths:
    - "User can 'End Game' and save result to history"
    - "User can view a list of all past games on a separate screen"
  artifacts:
    - path: "src/components/HistoryScreen.tsx"
      provides: "UI for viewing archived games"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/HistoryScreen.tsx"
      via: "State-driven rendering"
---

<objective>
Implement the "End Game" flow and the dedicated History screen.

Purpose: To allow users to conclude games and review past performance.
Output: A functional history view with archived game results and a clear conclusion process.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-persistence-history/04-CONTEXT.md
@.planning/phases/04-persistence-history/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement "End Game" Logic and Archiving</name>
  <files>src/App.tsx, src/components/Dashboard.tsx</files>
  <action>
    Add `history` state (array of archived games) to `App.tsx` and persist it to `localStorage`.
    Update `Dashboard.tsx` to include an "End Game" button (D-4-04).
    Implement `handleEndGame` in `App.tsx`:
    - Save the current game final scores and timestamp to the `history` array.
    - Clear the current game state (`players`, `transactions`).
    - Reset `gameStarted` to false.
  </action>
  <verify>
    <automated>grep -q "handleEndGame" src/App.tsx</automated>
  </verify>
  <done>Games can be concluded and archived for later viewing.</done>
</task>

<task type="auto">
  <name>Task 2: Create HistoryScreen Component</name>
  <files>src/components/HistoryScreen.tsx, src/components/HistoryScreen.css, src/App.tsx, src/components/SetupScreen.tsx</files>
  <action>
    Create `HistoryScreen.tsx` which displays a list of past games.
    For each game, show the date, player names, and final scores.
    Add a "View History" button to `SetupScreen.tsx` and the `Dashboard.tsx` header (if space permits) or as a separate tab.
    Update `App.tsx` to manage a new `view` state (setup, dashboard, history).
  </action>
  <verify>
    <automated>grep -q "HistoryScreen" src/App.tsx</automated>
  </verify>
  <done>History screen is functional and displays archived sessions clearly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| History Array -> Screen | Trusted internal state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Info Disclosure | History View | accept | Non-sensitive data |
</threat_model>

<verification>
- Start a game and end it using the "End Game" button.
- Go to the History screen and verify the game appears with the correct final scores.
- Refresh the browser on the history screen; verify it stays there or returns based on navigation state.
- Ensure the sum of archived scores is always 0.
</verification>

<success_criteria>
- Ending a game archives it to history.
- History view is accessible and shows correct session data.
- State persists after concluding a game.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistence-history/04-02-SUMMARY.md`
</output>
