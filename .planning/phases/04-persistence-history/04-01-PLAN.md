---
phase: 04-persistence-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/App.tsx]
autonomous: true
requirements: [SAVE-01]
must_haves:
  truths:
    - "App loads existing game state from localStorage on startup"
    - "App saves current state to localStorage on every change"
  artifacts:
    - path: "src/App.tsx"
      provides: "Persistence logic via useEffect"
---

<objective>
Implement auto-save and persistent state recovery using localStorage.

Purpose: To ensure the current game session survives browser refreshes and closings.
Output: Resumable game state that persists across sessions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-persistence-history/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement State Persistence in App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Add `useEffect` to save `players`, `transactions`, and `gameStarted` to `localStorage` under a key like `horse-score-game`.
    Add `useEffect` with empty dependency array to load the same state on mount.
    Handle cases where no state is found (clean start).
    Ensure the `gameStarted` flag is correctly handled to show either `SetupScreen` or `Dashboard` on reload.
  </action>
  <verify>
    <automated>grep -q "localStorage.setItem" src/App.tsx</automated>
  </verify>
  <done>Game state is persisted and recovered automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> State | Untrusted data stored in browser storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | App.tsx | mitigate | Use try-catch blocks and basic schema validation when parsing data from localStorage |
</threat_model>

<verification>
- Start a game with 4 names and perform a few transfers.
- Refresh the browser; verify the players and log are still there.
- Close the tab and reopen; verify the game is still active.
</verification>

<success_criteria>
- No loss of current game progress on page refresh.
- Score and history state are fully restored on app startup.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistence-history/04-01-SUMMARY.md`
</output>
